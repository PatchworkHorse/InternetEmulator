# Interior networks 

networks:
  horse-internal:
    driver: bridge
    internal: true
    ipam:
      config:
        - subnet: 10.0.0.0/16

  duck-internal:
    driver: bridge
    internal: true
    ipam:
      config:
        - subnet: 10.1.0.0/16

  ram-internal:
    driver: bridge
    internal: true
    ipam:
      config:
        - subnet: 10.2.0.0/16
